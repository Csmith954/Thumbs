This project will allow the user to enter a message that will then be encoded and returned back to them.
The cipher will first convert the users text into a binary string, it will then use the binary chars
to print out a thumbs up (if 1) or a thumbs down (if 0).